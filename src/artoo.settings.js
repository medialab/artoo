;(function(undefined) {
  'use strict';

  /**
   * artoo settings
   * ===============
   *
   * artoo settings that may be set by user.
   */
  artoo.settings = {
    store: {
      engine: 'local'
    },
    log: {
      enabled: true,
      level: 'verbose'
    },
    instructions: {
      record: true
    }
  };
}).call(this);
